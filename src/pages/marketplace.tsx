import CreatorStudioLayout from '@components/common/CreatorStudioLayout';
import MarketPlaceIndex from '@components/creator-studio/marketplace';
import React from 'react';

const marketplace = () => {
  return (
    <CreatorStudioLayout>
      <MarketPlaceIndex />
    </CreatorStudioLayout>
  );
};

export default marketplace;
