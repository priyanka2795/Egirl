version: '3'
services:
  web:
    container_name: egirls_frontend_container
    image: egirls_frontend_image
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/.next
